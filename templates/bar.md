# The bar — what 10/10 means for THIS app

> The fitness function the loop measures against. Without this, "drive toward perfection" has no
> direction and the loop drifts back to clearing easy items. Fill it per app. The loop may refine it
> (step 7), you own the big strokes.

**App:** <name> — <one line: who it is for and the one job it must do superbly>

**The feeling at 10/10:** <one paragraph. What does a delighted user/agent say after using it? What
makes them choose it again over the alternative? Be specific to this app, not generic.>

## Dimensions (score each 1-10, honestly, with a today-date)

Keep these few and real. A dimension you never act on is noise, cut it. Add ones this app lives or
dies on (a game: moment-to-moment feel, sense of progression, surprise. A business app: time-to-value,
trust, how little it makes the user think).

| Dimension | Now | What 10 looks like here |
|---|---|---|
| First-run / onboarding | ? | A new user reaches the core value in one move, with no confusion. |
| Core loop / main job | ? | The thing the app exists for is fast, obvious, and feels good every time. |
| Polish & coherence | ? | Type, spacing, motion, copy all feel considered. Nothing looks placeholder or off. |
| Performance & responsiveness | ? | Instant to the hand. Nothing janks, blocks, or makes the user wait without reason. |
| Robustness | ? | Edge cases, empty states, errors and bad input are handled gracefully, never ugly. |
| Agent-usability | ? | An AI agent could drive the whole app via clear structure / APIs / labels. |
| <app-specific> | ? | <...> |

**Current overall:** <N>/10 as of <date>. **Biggest known gap:** <one line, the loop updates this>.

## Reference set — what we measure against (the loop fills and refines this)

The named competitors and best-in-class apps this app is benchmarked against, plus the
current-practice questions worth re-checking against the live world periodically (not from memory —
model knowledge goes stale). An outward-lens tick studies one of these, finds the gap, and may raise
a dimension above. If this is still empty, the next outward tick's first job is to research and fill it.

- **Best-in-class / competitors:** <e.g. for an image studio: Midjourney, Playground, Photoroom — and the one thing each does better than us>
- **Current-practice questions to re-verify periodically:** <e.g. is there a better model/API than the one we use now? what's the current bar for this interaction pattern? has the platform shipped something that obsoletes our workaround?>
- **Last looked outward:** <date + what it changed>

## Out of scope / deliberate non-goals

<What you are NOT trying to be. This stops the loop from "improving" the app toward something it
shouldn't become, the other face of satisficing: busywork that adds the wrong things. Also the guard
against outward research pulling you to chase every shiny thing a competitor has.>
